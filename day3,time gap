#include <stdio.h>
int main()
{
	printf("请输入时间");
	int hour1, m1;
	int hour2, m2;

	scanf_s("%d %d", &hour1, &m1);
	scanf_s("%d %d", &hour2, &m2);

	int time1 = hour1 * 60 + m1;
	int time2 = hour2 * 60 + m2;
	int t = time2 - time1;

	printf("时间相差%d小时%d分钟", t / 60, t % 60);

	return 0;
	
}
